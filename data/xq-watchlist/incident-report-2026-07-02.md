# 南京路彼岸自选股监控"登录失效"事件报告

## 时间
2026-07-02 13:33 用户反馈监控提示登录失效

## 调查结论
**并非登录失效**。根因是目标用户（南京路彼岸，uid=7273692735）在 2026-07-02 09:40:08 一次性移除了其自选股列表中的全部 6 只股票，导致监控系统检测到自选股数量从 6 只变为 0 只。

## 证据

### 1. 自选股变动日志（changelog.json）
2026-07-02 09:40 共 6 条移除记录：
- 株冶集团 (SH600961)
- 三美股份 (SH603379)
- 有研硅 (SH688432)
- 多氟多 (SZ002407)
- 江丰电子 (SZ300666)
- 天阳科技 (SZ300872)

### 2. 监控日志（monitor.log）
```
[09:40:08] 执行检测...
[INFO] 当前自选股: 0 只
[CHANGE] 新增=0 移除=6 (6 → 0)
```

### 3. 登录态检查
当前登录态有效：
- cookie 文件：~/.xueqiu_browser_state.json，已刷新
- xq_a_token / xq_id_token / xq_r_token 均存在
- API 调用正常返回 `error_code=0`，只是 `data.stocks=[]`

## 关于"登录失效"告警的误报
11:50:39 出现一次 SSL 握手超时：
```
[ERROR] 登录态失效: 验证请求失败: <urlopen error _ssl.c:1015: The handshake operation timed out>
[OK] 企微推送成功
```

该告警由 `run()` 函数内的服务端登录态验证触发。由于 `loop_run()` 和 `run()` 重复进行服务端验证，第二次验证时恰好遇到 SSL 超时，被误判为登录失效并发送了企微告警。

## 已采取的措施
1. 已刷新雪球登录态 cookie
2. 已修改 `scripts/xq_watchlist_monitor.py`：
   - `run()` 在 loop 模式下接收已由 `loop_run()` 验证过的登录态，避免重复服务端验证
   - 非 loop 模式下仍做本地登录态检查
3. 已重启监控进程（PID 26669），运行正常
4. 脚本语法检查通过

## 当前状态
- 监控进程：运行中
- 自选股数量：0 只（目标用户真实清空，非系统故障）
- 下次检测：按 5 分钟间隔继续轮询
